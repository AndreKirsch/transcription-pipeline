# transcription-pipeline
Automated pipeline that pulls call recordings from Dropbox, transcribes them via OpenAI, and stores structured results in MongoDB—driven by a YAML workflow with Zod validation, Pino logging, and Mongo-backed idempotency.
